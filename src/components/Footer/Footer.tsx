import { Divider } from '@chakra-ui/react'
import React, { FC } from 'react'

interface IProps {

}

export const Footer: FC<IProps> = ({  }) => {
  return (
    <div>
      <Divider />
    </div>
  )
}